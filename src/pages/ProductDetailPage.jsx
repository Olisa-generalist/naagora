// src/pages/ProductDetailPage.jsx
// Full product detail + order screen — built in next session
export default function ProductDetailPage() {
  return <div className="page"><div className="page-content"><p style={{padding:'40px 0',textAlign:'center',color:'var(--text-3)'}}>Product detail — coming next</p></div></div>
}
